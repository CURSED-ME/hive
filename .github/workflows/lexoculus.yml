name: LexOculus Guardian

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  compliance-scan:
    name: EU AI Act Compliance Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Optical Audit Scan
        id: scan
        env:
          LEXOCULUS_API_URL: ${{ vars.LEXOCULUS_API_URL || 'https://lexoculus.com' }}
          WEBHOOK_SECRET: ${{ secrets.LEXOCULUS_WEBHOOK_SECRET }}
          WEBHOOK_PAYLOAD: ${{ toJson(github.event) }}
        run: |
          # Write payload to file safely
          echo "$WEBHOOK_PAYLOAD" > payload.json

          # Calculate signature
          SIGNATURE=$(openssl dgst -sha256 -hmac "$WEBHOOK_SECRET" payload.json | cut -d' ' -f2)

          # Post to LexOculus (Added -L and debug logging)
          RESPONSE=$(curl -s -L -X POST \
            "${LEXOCULUS_API_URL}/api/webhooks/github" \
            -H "Content-Type: application/json" \
            -H "X-GitHub-Event: pull_request" \
            -H "X-Hub-Signature-256: $SIGNATURE" \
            --data @payload.json)
          
          echo "ComplianceAI Response: $RESPONSE"
          
          echo "response=$RESPONSE" >> $GITHUB_OUTPUT
          
          # Parse risk
          RISK_LEVEL=$(echo $RESPONSE | jq -r '.risk_level // "MINIMAL_RISK"')
          BLOCKED=$(echo $RESPONSE | jq -r '.blocked // false')
           
          echo "## 👁️ LexOculus Audit Results" >> $GITHUB_STEP_SUMMARY
          echo "**Risk Level:** $RISK_LEVEL" >> $GITHUB_STEP_SUMMARY
          echo "**Blocked:** $BLOCKED" >> $GITHUB_STEP_SUMMARY
          
          if [ "$BLOCKED" = "true" ]; then
            echo "❌ PR blocked due to UNACCEPTABLE risk under EU AI Act"
            exit 1
          fi
